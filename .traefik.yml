displayName: Robots.txt Plugin
type: middleware
iconPath: .assets/icon.png

import: github.com/foxxmd/traefik-plugin-robots-txt

summary: 'Traefik plugin to create, overwrite or complete the robots.txt file'

testData:
  aiRobotsTxt: true
  customRules: "\nUser-agent: *\nDisallow: /private/\n"
